on:
  push:
    branches:
      - master

jobs:
  flake8:
    runs-on: ubuntu-latest
    steps:
      - name: Install flake8
        run: python -m pip install flake8
      - name: Flake8 checks
        run: python -m flake8 .
